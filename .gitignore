# Rust build output
/target/
/pdf-bin/target/

# Native PDF binaries (built by `deno task build`)
/bin/

# Deno lock file (commit if you want reproducible installs)
deno.lock

# macOS
.DS_Store

node_modules

# IA
.claude/settings.local.json
